services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: taskbeacon
    ports:
      - "7001:5432"
    volumes:
      - taskbeacon_pgdata:/var/lib/postgresql/data

volumes:
  taskbeacon_pgdata:
